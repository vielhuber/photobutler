{
    "version": "1.0.1",
    "private": true,
    "scripts": {
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "devDependencies": {
        "prettier": ">=3",
        "@prettier/plugin-php": ">=0.25"
    }
}
